// The site uses one fixed (dark) theme. Several optional embeds (ECharts, Plotly, Mermaid,
// diff2html, Vega-Lite, Jupyter embeds, search, tables) read this at load time to pick their
// dark-themed variant.
let determineComputedTheme = () => "dark";
